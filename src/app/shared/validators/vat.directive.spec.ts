import { VATDirective } from './vat.directive';

describe('VATDirective', () => {
  it('should create an instance', () => {
    const directive = new VATDirective();
    expect(directive).toBeTruthy();
  });
});
